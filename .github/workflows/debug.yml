name: Debug Test
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check workflow
        run: |
          echo "Debug workflow triggered at $(date)"
          echo "Event: ${{ github.event_name }}"
          echo "Repository: ${{ github.repository }}"
